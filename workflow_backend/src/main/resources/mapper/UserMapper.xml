<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.example.LogicCircuit.mapper.UserMapper">

    <select id="findByEmail" parameterType="string" resultType="com.example.LogicCircuit.entity.User">
        SELECT * FROM users WHERE email = #{email}
    </select>

    <insert id="insertUser" parameterType="com.example.LogicCircuit.entity.User" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO users (name, email, password, created_at, updated_at)
        VALUES (#{name}, #{email}, #{password}, #{createdAt}, #{updatedAt})
    </insert>

</mapper>
